// Override webpack configuration to support older React apps in newer Node environments
module.exports = function override(config, env) {
  // Add custom webpack configuration here if needed
  return config;
};
